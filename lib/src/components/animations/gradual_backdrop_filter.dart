import 'package:flutter/material.dart';

class GradualBackdropFilter extends StatelessWidget {
  const GradualBackdropFilter({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
